/**
 * 控制器层， 主要存放控制器层类，如果分业务类型，
 * 可在此包内重新建立新包，用于存放具体业务控制层类。
 * 原则上控制器层只处理基本等校验规则，所有的业务逻辑都需要下放到Service层和Domain层。
 *
 * app 目录用于app端的控制器层
 * web 目录用于web或h5页面的控制器层
 *
 */

package ${package}.application;


